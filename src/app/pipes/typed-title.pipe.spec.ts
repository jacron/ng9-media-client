import { TypedTitlePipe } from './typed-title.pipe';

describe('TypedTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new TypedTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
