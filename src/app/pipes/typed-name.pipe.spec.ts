import { TypedNamePipe } from './typed-name.pipe';

describe('TypedNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TypedNamePipe();
    expect(pipe).toBeTruthy();
  });
});
