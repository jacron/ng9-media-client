import { StartletterPipe } from './startletter.pipe';

describe('StartletterPipe', () => {
  it('create an instance', () => {
    const pipe = new StartletterPipe();
    expect(pipe).toBeTruthy();
  });
});
